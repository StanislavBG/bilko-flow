# Hello World

## Overview
A minimal "Hello World" static HTML page served via Python's built-in HTTP server on port 5000.

## Project Structure
- `index.html` — The single HTML page displaying "Hello World"
- `server.py` — Simple Python HTTP server with no-cache headers

## Recent Changes
- 2026-02-10: Initial creation — bare "Hello World" page
